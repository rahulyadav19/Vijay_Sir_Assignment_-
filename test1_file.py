
num_of_values= input("Enter number of key-value pairs")
type(num_of_values)

tot= num_of_values*2
i=0
for i in tot:
    print (i)
    keys=input("Enter Key")
    values=input("Enter value")
    i=i+1


for line in list:
    id = line[0]
    values = line[1]

# This didn't work. It says not iterable
